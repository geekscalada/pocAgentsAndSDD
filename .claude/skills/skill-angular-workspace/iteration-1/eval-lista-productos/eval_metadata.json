{
  "eval_id": 1,
  "eval_name": "eval-lista-productos",
  "prompt": "Tengo este endpoint en mi spec: `GET /productos` devuelve un array de `{id, nombre, precio}`. Genera el código Angular de ejemplo para mostrarlos en una lista.",
  "assertions": [
    {
      "id": "no_ngmodule",
      "description": "El código generado NO contiene NgModule"
    },
    {
      "id": "standalone_true",
      "description": "El componente declara standalone: true"
    },
    {
      "id": "uses_inject_fn",
      "description": "Usa inject(HttpClient) en lugar de inyección por constructor"
    },
    {
      "id": "uses_signals_or_toSignal",
      "description": "Usa signal() o toSignal() para gestionar el estado reactivo"
    },
    {
      "id": "new_control_flow",
      "description": "El template usa @for y/o @if, no *ngFor/*ngIf"
    },
    {
      "id": "typed_interface",
      "description": "Define una interfaz TypeScript tipada para Producto con id, nombre y precio"
    }
  ]
}
